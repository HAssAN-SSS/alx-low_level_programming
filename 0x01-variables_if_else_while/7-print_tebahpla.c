#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
int i = 26;
for (i = 26; i <= 0; i--)
{
putchar('a' + i);
}
putchar('\n');

	return (0);
}
